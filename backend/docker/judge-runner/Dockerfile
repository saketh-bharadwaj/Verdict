FROM node:20-alpine

WORKDIR /sandbox

# Future runner image for untrusted code execution.
# In production this image would contain language runtimes,
# seccomp/apparmor profiles, cgroup limits, and a restricted filesystem.
CMD ["sh", "-c", "echo Judge runner placeholder image"]
